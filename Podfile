# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'RecipeTimer' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RecipeTimer

	pod 'TinyConstraints'
	pod 'QuickTableViewController'

  target 'RecipeTimerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RecipeTimerUITests' do
    # Pods for testing
  end

end
